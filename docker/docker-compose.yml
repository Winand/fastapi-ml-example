services:
  fastapi_ml_example:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      # args:
      #   BASE_IMAGE: [python:3.13-slim (Debian)] | python:3.13-alpine (BusyBox) | almalinux:10 (Fedora)
      #   UV_IMAGE: ghcr.io/astral-sh/uv:0.12.0
      #   PYPI_MIRROR: https://pypi.org
    restart: unless-stopped
    ports:
      - 8000:8000
    environment:
      WEB_CONCURRENCY: 4
